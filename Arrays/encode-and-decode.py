from typing import List


def encode(strs: List[str]) -> str:
    # 4#
    for str in strs:
        current = []
        res += str(len(str))+'#'+str
    return res


def decode(s: str) -> List[str]:


)
